{
  "name": "index",
  "version": "1.0.0",
  "description": "czz swap sdk",
  "source": "src/index.js",
  "main": "dist/index.js",
  "scripts": {
    "build": "microbundle -o dist/ --sourcemap false --no-compress",
    "dev": "microbundle watch -o dist/ --sourcemap false --no-compress --jsx React.createElement",
    "api:doc": "typedoc --out docs src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "microbundle": "^0.13.3",
    "typedoc": "^0.21.4",
    "typescript": "^4.3.5"
  }
}
